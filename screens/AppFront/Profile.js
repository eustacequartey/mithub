import React from 'react';
import { View, StyleSheet, Text } from 'react-native';

const Profile = () => {
  return (
    <View style={styles.wrapper}>
      <Text style={{ fontSize: 100 }}>hallo</Text>
    </View>
  );
};

export default Profile;
const styles = StyleSheet.create({
  wrapper: {},
});
